version: '3'

tasks:
  dev:
    desc: Start development environment
    cmds:
      - ./bin/start-dev.sh

  build:
    desc: Build image
    cmds:
      - docker-compose -f docker-compose.yml build frontend

